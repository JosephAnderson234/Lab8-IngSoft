# Dependencias de ejecucion (brokers reales).
# El nucleo y las pruebas NO las requieren: los adaptadores las importan
# de forma perezosa, por lo que el flujo en memoria corre sin instalarlas.
pika>=1.3,<2.0
confluent-kafka>=2.3,<3.0
